# GymLink UI

This directory is reserved for the two approved Flutter clients:

```text
ui/
  gymlink_mobile/   # Android: Member and Trainer
  gymlink_desktop/  # Windows: GymAdmin and CentralAdmin
```

The client projects will be created in their implementation phase. Shared
business logic remains in the backend API; clients consume typed API contracts
and preserve backend validation messages.
